主要弄明白，什么时候编译器会提供默认的构造和拷贝构造？

编译器提供的默认构造和拷贝构造都干了啥？

构造函数和拷贝构造都是什么时候被调用的？

这三个问题弄明白了，就不容易写带Bug的程序了。

哦，还要弄明白什么是默认构造函数，C++里面默认构造函数的概念可能和你一直理解的有点出入。

默认构造函数是在被调用时不需要传递实参的构造函数。

这就包括没有形参的构造函数以及有形参但是每个形参都有默认值的构造函数。这是需要注意的点。