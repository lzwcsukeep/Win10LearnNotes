sed 中针对模式空间，执行多个命令的方式有很多，选择一种你觉得方便的就行。

1. 使用多个 -e 选项指定

2. 用分号 ； 分隔多个 command

3. 使用newline 字符 分隔多个 command 

4. -f 选项指定的文件中的commands 使用换行符隔开。

5. 还可以使用大括号{com1;com2;...} 将多个commands 看成一个整体。

但一定要记住sed 的工作原理， 这些commands 拼接在一起，然后作用在pattern space 上的。