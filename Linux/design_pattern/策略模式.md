🧠 概念

定义一系列算法（策略），把它们封装起来，让它们可以互相替换。
—— 用多态消除 if/else。

🎯 示例场景

比如一个支付系统，有不同的支付方式：

```c
class PaymentStrategy {
public:
    virtual void pay(double amount) = 0;
    virtual ~PaymentStrategy() = default;
};

class WeChatPay : public PaymentStrategy {
public:
    void pay(double amount) override {
        cout << "Paid " << amount << " via WeChat\n";
    }
};

class Alipay : public PaymentStrategy {
public:
    void pay(double amount) override {
        cout << "Paid " << amount << " via Alipay\n";
    }
};

class PaymentContext {
    unique_ptr<PaymentStrategy> strategy;
public:
    PaymentContext(unique_ptr<PaymentStrategy> s) : strategy(move(s)) {}
    void execute(double amount) { strategy->pay(amount); }
};

int main() {
    PaymentContext ctx(make_unique<WeChatPay>());
    ctx.execute(100);

    ctx = PaymentContext(make_unique<Alipay>());
    ctx.execute(200);
}

```

✅ 特点：

- 用多态代替 if...else...；
- 算法可自由替换；
- 符合开闭原则。

### 💡增加一个支付策略

比如我们要新增 信用卡支付（CreditCardPay）

👉 按照开闭原则（对扩展开放，对修改关闭），
我们不改动任何旧代码，
只要新增一个类即可。

✅ 新增策略类：CreditCardPay
```c
class CreditCardPay : public PaymentStrategy {
public:
    void pay(double amount) override {
        cout << "Paid " << amount << " via Credit Card\n";
    }
};
```

✅ 使用新策略

```c

int main() {
    PaymentContext ctx1(make_unique<WeChatPay>());
    ctx1.execute(100);

    PaymentContext ctx2(make_unique<Alipay>());
    ctx2.execute(200);

    PaymentContext ctx3(make_unique<CreditCardPay>());
    ctx3.execute(300);
}
```

输出结果：
```plaintext
Paid 100 via WeChat
Paid 200 via Alipay
Paid 300 via Credit Card
```

总结：

策略模式主要有三部分：

- Strategy（抽象策略接口）	  定义所有算法（或行为）的统一接口
- ConcreteStrategy（具体策略类）	实现不同算法逻辑
- Context（上下文类）	  持有 Strategy 指针，负责调用策略

新增一个策略，只需要添加一个新类，并且使用新的策略即可。
不需要修改旧代码，符合开闭原则。