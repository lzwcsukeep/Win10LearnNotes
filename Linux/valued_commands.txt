find 命令新理解：

基本结构：find [起始目录] [查找条件] [执行动作]

起始目录：从哪个目录开始查找，比如 . 表示当前目录，/ 表示根目录，不写默认为"."
查找条件：按名称、类型、大小、时间等条件筛选文件
执行动作：对找到的文件执行操作（可选）

如： find . -name "*.txt"   // 比如找所有 .txt 文件 "-name "*.txt"" 就是查找条件。 
