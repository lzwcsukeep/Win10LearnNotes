文本中含有各种字符，其中空白符(whitespace)的概念不仅仅限于空格(space)。

whitespace 包括 空格(space),制表符(tab)，换行符(\n)和回车(\r)。

正则表达式中表示一个空白符(space,tab,\n,\r中任一个)可以使用` \s `。

总结：即注意区分空白符和空格的区别。空白符(whitespace) 包括了空格(space),此外还有制表符(tab),换行符(\n),回车符(\r)。
正则表达式中使用`  \s `表示一个空白符。