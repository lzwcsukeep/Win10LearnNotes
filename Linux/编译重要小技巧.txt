1. make -n 会打印构建命令而不真正执行。你可以观察 -DDEBUG 或 --coverage 是否出现在编译命令行里。

2. 关于编译过程宏的来源渠道。
方式1：命令行指定。
make DEBUG=1
make RELEASE=1

方式2： 环境变量
export DEBUG=1
make

方式3： CI/CD 或打包脚本注入
比如自动化构建工具里，有类似：
make DEBUG=1 mod=server

因此代码中出现某个宏，但是未见定义，不代表这个宏就不会有值，因为有其他外部来源可以定义宏。