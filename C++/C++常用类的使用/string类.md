string 类的对象表示一个字符串。

构造函数：

![](C:\HOME\Files\Win10LearnNotes\img_src\2024-04-24-22-19-15-image.png)

可以空，可以用另一个字符串构造。还可以使用另一个字符串的一部分构造。还可以用C风格的字符串构造。

有用的函数：

find() : 参数指定的字符串，完全匹配 

find_first_of() ： 找到参数中任意一个就行

这两个函数不一样。

详细的看cplusplus string
